Download each code as a .R file. To run the code, hit Cmd + A (select all) and Cmd + Enter (to run the code). Make sure to change the file path to match your file location. Download all required R packages before running.  
# RUN THIS FIRST — it opens a file picker. Navigate to your file and select it.  
file.choose()  
Copy whatever path it returns, then paste it into the script below where it says my file path.
